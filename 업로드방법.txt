GitHub 업로드 (폴더째 드래그!)
1. zip 압축해제 → api/, public/ 폴더 확인
2. GitHub 레포(unisquads) 기존 파일 전부 삭제
3. Add file → Upload files → api·public 폴더 통째로 드래그
4. Commit → 자동 재배포

이번 수정 (시도 코드 회피):
- admin_regn1 = "all" (시/도 코드 매칭 안 씀)
  → 강원특별자치도·전북특별자치도 등 명칭변경 문제 원천 차단
- swrd에 시군구 포함 ("구로구 삼환로즈빌 102동 101호")
  → 흔한 건물명이 타 지역에 있어도 시군구로 좁힘
- juso가 시군구까지 정제하므로 그 값 그대로 활용
- 단일: 동·호 입력 판 깔아줌(집합건물 자동펼침)
- 일괄: 엑셀 주소 그대로(동·호 섞여있으면 자동 파싱)
