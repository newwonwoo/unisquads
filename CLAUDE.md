# 이 저장소에서 일할 때

주소 정제(JUSO → 네이버 → PNU 확정) + 등기고유번호(IROS) 조회 웹앱이다.
브라우저에서 전부 돌고, 서버는 Vercel의 얇은 파이썬 프록시뿐이다.

**작업을 시작하기 전에 [`인수인계.md`](인수인계.md)를 읽어라.** 지금 무엇이 측정으로
확인된 사실이고 무엇이 아직 가설인지, 다음에 뭘 해야 하는지가 거기에 있다.

## 반드시 지킬 것

1. **이미 확정된 행을 회귀시키지 않는다.** 규칙을 완화할 때는 완화가 기존
   CONFIRMED 행을 몇 건 뒤집는지 먼저 재고, 0이 아니면 그 규칙은 그대로 넣지
   않는다. 이 저장소의 복구 규칙은 전부 이 검사를 통과한 것들이다.

   **"몇 건 뒤집는지"는 반드시 `npm run audit:regression`으로 잰다.**
   배치를 다시 돌려 결과 엑셀을 비교하는 방식으로는 잴 수 없다 — 확정 행은
   `isProtectedIrosSuccess`/`isProtectedAddressSuccess`가 잠가서 애초에
   재평가되지 않으므로, 그 비교의 "회귀 0건"은 *잠금이 작동했다*는 뜻일 뿐
   *규칙이 안전하다*는 뜻이 아니다. 이 구분을 흐린 적이 있고 문제가 됐다.

2. **회귀 리스크는 네 층위로 잰다.** 전처리(`npm run audit:preprocess` —
   실행 원본주소 19,819건 파싱 기준선) · 공통모듈(`npm run audit:common` —
   원시함수 출력 7,523건 기준선) · 개별모듈(회귀 감사 + 단위 반례) ·
   모듈배치(간섭 감사 2종: `audit:interference` 등기 사다리,
   `audit:address` 정제율 체인). 판정을 바꾸는 규칙은 겹으로 막는다 —
   어느 하나만으로는 부족하다.
   - `npm run audit:regression` — 실측 후보 코퍼스(24개 지번·11,808건 판정)에
     운영 판정 함수(`public/unit-decision.mjs`)를 그대로 돌려 기준선과 비교한다.
     넓게 훑지만, **실측에 없는 위험은 못 본다**(예: 다른 동을 가리키는 접두 호는
     실제 데이터에 없어 감사가 잡지 못했다).
   - `npm run audit:interference` — 사다리 모듈을 하나씩 차단해 "이 모듈이
     없었으면 다른 모듈이 다른 고유번호를 냈는가"를 전수로 잰다. 충돌은 배열
     조정으로 해소되지 않는다(순서를 바꾸면 반대쪽 답이 이길 뿐) — 근거 약한
     모듈이 그 조건에서 기권하도록 고쳐야 한다(무회귀 모듈).
   - 단위 테스트의 **반례** — 규칙마다 "이 조건에서는 절대 발화하지 않는다"를
     직접 고정한다. 좁지만 실측에 없는 위험을 잡는 건 이쪽뿐이다.

   기준선이 바뀌면 `npm run audit:update`로 갱신하되, **무엇이 왜 달라졌는지
   커밋에 남긴다**. 갱신은 "이 판정 변화를 의도했다"는 선언이다.
   미확정→확정으로 바뀐 판정도 자동으로 개선이 아니다 — 근거 없는 오확정일 수
   있으므로 표본을 프로브로 확인한 뒤 갱신한다.

   새 실패 유형을 프로브했으면 `npm run corpus:build -- <저장본.json>`으로
   코퍼스에 넣어 감사 범위를 넓힌다.
3. **버전 올림은 손으로 한다.** `public/version.mjs`의 `APP_VERSION`과
   `CHANGELOG` 맨 앞에 한 줄. `scripts/bump-version.mjs` 참고.
4. **커밋 전 `npm test`와 `npm run test:iros` 둘 다 통과해야 한다.**
   (`npm test`는 회귀 감사를 먼저 돌리고, 감사가 실패하면 거기서 멈춘다.)
5. **추정과 측정을 섞어 쓰지 않는다.** "몇 행이 복구된다"고 쓸 때는 그게 실제
   실행으로 잰 값인지, 재매칭 대상이 된 행 수인지(= 복구된다는 보장이 아님)를
   문장에 명시한다. 이 구분을 흐린 적이 있고 문제가 됐다.

## 명령

```
npm test              # 회귀 감사 + 단위·계약 테스트 (현재 346개 통과)
npm run audit:regression  # 실측 코퍼스로 판정 회귀만 검사 (11,808건)
npm run audit:interference  # 모듈 간 충돌 검사 — 순서가 고유번호를 바꾸면 실패
npm run audit:update      # 판정 변화를 의도했을 때만 기준선 갱신
npm run corpus:build -- <프로브저장본.json>   # 감사 코퍼스에 새 지번 추가
npm run test:iros     # IROS 세대매칭 E2E — 브라우저 띄워 실제 앱을 돌린다
npm run probe:iros    # 실제 등기 브리지에 직접 붙어 후보와 매칭 판정을 본다
npm run build         # build-info.js 생성 (배포 때 Vercel이 실행)
```

## 네트워크

세션에서 나가는 트래픽은 전부 정책 게이트웨이를 지난다. 허용 목록에 없는
호스트는 CONNECT 단계에서 403이다. 우회로는 없다 — 프록시를 빼고 443으로
직결해도 같은 게이트웨이가 받는다(인증서 발급자가 Anthropic이다).

막혀 있으면 우회하지 말고 그 사실을 보고해라. 무엇이 막혔는지는 이걸로 본다:

```
curl -sS "$HTTPS_PROXY/__agentproxy/status"
```

MCP 커넥터(네이버 지역검색, 카카오 로컬 등)는 세션 네트워크가 아니라
Anthropic 서버를 거쳐 나가므로 허용 목록과 무관하게 동작한다. 외부 사실
확인이 필요하면 먼저 이쪽을 써봐라.
