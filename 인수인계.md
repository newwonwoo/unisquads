# 인수인계 — 2026-08-07 기준 (v1.8.0)

> **2026-08-07 갱신**: 0~2단계 완료. 실패 상위 14곳을 실제 브리지
> (`https://unisquads.vercel.app/api`)로 전수 프로브했고(§8), 실측 근거로
> 매칭 규칙 4종을 추가했으며(§9), E2E 후보를 합성값에서 실측값으로 교체했다.
> 아래 §1~§7은 v1.7.2 시점의 기록 그대로 두고, 새 사실은 §8~§10에 있다.

새 세션은 이 대화를 물려받지 못한다. 레포 클론만 받는다. 그래서 지금까지의
판단 근거를 여기에 남긴다.

**이 문서의 원칙: 잰 것과 짐작한 것을 구분해서 쓴다.** 아래에 "실측"이라고
쓴 값은 20,023행 스냅샷에 규칙을 적용해 실제로 센 수다. "대상"이라고 쓴
값은 규칙이 손댈 수 있는 행 수일 뿐 복구된다는 보장이 아니다.

---

## 1. 지금 어디까지 왔나

20,023행 1회 실행(주소+등기, 약 1시간 30분) 결과를 놓고 실패를 전수로 뜯어
규칙을 아홉 번 나눠 넣었다. main은 v1.7.2다.

| PR | 내용 | 버전 |
|---|---|---|
| #34 | 붙어 온 법정동 인식 + 리 일치 시 읍면 차이 허용 | 1.3.0 |
| #35 | 지번+건물명 일치 시 법정동 오기 교정 + 있을 수 없는 층 절단 | 1.4.0 |
| #36 | 건물명 위생 + 동소 기준행 복제 + 2자 고유명 후보 좁히기 | 1.5.0 |
| #37 | IROS 세대매칭 4종 | 1.6.0 |
| #38 | IROS 동 표기 2종 | 1.6.1 |
| #39 | IROS E2E 회귀 테스트 | 1.6.2 |
| #40 | PNU 없는 IROS 조회 + 소재지 역확정 + 검증 세대 전파 | 1.7.0 |
| #42 | 건물명이 `동`으로 시작할 때 앞 지번 유실 수정 | 1.7.1 |
| #41 | 실제 등기 브리지 프로브 | 1.7.2 |

테스트는 전부 통과한다: 단위·계약 316개, IROS E2E 11행/0실패.

---

## 2. 주소 쪽 — 여기는 측정이 끝났다

**363행 회수, 기존 확정 행 회귀 0건.** 20,023행 전량에 규칙을 적용해 직접
셌다. 내역:

- **272행** — 붙어 온 법정동(`진관리642-2산내들아파트101-102`)을 검증 후보로
  인정 + 리가 맞으면 읍면 차이를 차단하지 않음. 행정구역 개편(당진군 송산면 →
  당진시 고대면)으로 읍면만 어긋나던 건들이다.
  `public/address-recovery-rules.mjs`의 `gluedAdminToken` / `blockingRegionLevel`.
- **84행** — 지번과 건물명이 둘 다 맞는데 법정동만 원문이 틀린 경우 결과 쪽
  법정동을 채택. `public/address-multilot-rules.mjs`의 `canAcceptLotBuildingCorrection`.
- **7행** — `456-19301호`처럼 끊기가 애매한 값에서 93층이 나오면 사람은 바로
  301호로 읽는다. 60층 초과가 나오는 절단은 거부한다.
  `public/address-quality-rules.mjs`의 `splitImplausibleFloorHo`.
  이 중 1건은 67층으로 이미 잘못 확정돼 있던 행이었다.

### 회귀 검사에서 실제로 걸린 것들 (같은 실수 반복 금지)

- 붙은 토큰 띄어쓰기를 **일반화**해서 풀었더니 700행이 바뀌고 그중 425행이
  이미 CONFIRMED였다. `상동5길`(도로), `에이동101호`(알파벳 동)가 깨졌다.
  → 대표값(`out.bjd`)은 절대 안 건드리고 **검증 후보에만** 추가하는 방식으로 선회.
- 그래도 `상가동044호`가 1건 뒤집혔다. → 추출한 머리말에 `isBuildingPartToken`
  가드 추가.
- `stripRegistryTail`이 `거제`를 `거`로 먹었다. → 2자 미만이 되면 원본 유지.

---

## 3. IROS 쪽 — 여기가 열려 있는 문제다

### 3-1. 수집은 멀쩡하다. 매칭이 실패한다.

18,178행에서 `총건수 = 파싱수 = 고유후보수`이고 완전여부가 Y다. 즉 등기소에서
받아온 후보 목록 자체는 빠짐이 없다. 그 완전한 후보에서 우리 동·호로 고르는
단계가 실패한다. **재조회가 아니라 재매칭으로 풀 문제다.**

미해결 1,860행이 건물 197곳에 몰려 있다. 상위 15곳이 71.5%다.

### 3-2. 넣은 규칙 여섯 가지 — 1,238행이 "재매칭 대상"

**주의: 1,238행은 복구된 행 수가 아니다.** 규칙이 손댈 수 있는 행 수다.
실제로 몇 건이 확정으로 바뀌는지는 **다시 돌려봐야 안다.**

| 유형 | 대상 | 규칙 |
|---|---|---|
| 층 유실 (`101동 3층1호`를 호 `1`로만 읽음) | 136행 | `rawUnitRecoveryVariants` |
| 추정 동 무시 (`서도아파트 102`의 102는 호) | 94행 | `selectDongAgnosticHoCandidate` |
| 복수지번 소재지 (확정 지번으로 거르면 전멸) | 194행 | `R-IROS-LOT-FALLBACK` |
| 중복 동 표기 (`106동 1동 102호`) | 40행 | `raw_duplicate_dong` |
| 등기부 동 체계 불일치 (원문 302동 ↔ 등기부 가동·나동) | 45행 | `R-IROS-DONG-AGNOSTIC` |
| 지하 표기 (B1 / 지1 / 지하1) | — | `rawUnitRecoveryVariants` |

`public/unit-match.mjs`(`MATCHER_VERSION = "iros-matcher-v11"`),
`public/app.js`의 R-IROS-* 지점, `public/failure-recovery-plan.mjs`.

**순서 계약이 있다**: `R-IROS-DONG-AGNOSTIC-HO`는 프로필 복구 **뒤**에 와야
한다. 동을 무시하는 매칭은 최후 수단이라 앞에 두면 멀쩡한 매칭을 먹는다.
`tests/test_pnuless_iros.mjs`가 이 순서를 강제한다.

### 3-3. 일곱 번째 원인 — 등기부에 그 동이 아예 없다

18곳 / 663행. 단지가 여러 지번에 걸쳐 있어서 확정 지번 하나로만 조회하면
등기부에 그 동이 안 나온다. 이 안에서 **`extractExplicitLotRefs` 정규식 버그로
342행의 지번이 통째로 유실되던 것**을 찾아 고쳤다(#42). 원인은
`414-2 동산아파트`의 `동산`을 "동 표기"로 읽어 `414-2`까지 후보에서 뺀 것.

```js
// public/address-multilot-rules.mjs
const number = String.raw`산?\s*\d+(?:-\d+)?(?![\d-])(?!\s*[동호층](?![가-힣]))`;
//                                                                 ^^^^^^^^^ 뒤에 한글이 오면 건물명이다
```

이건 네이버 지역검색 MCP로 광주 동산아파트의 실제 지번을 확인해서 잡았다.
외부 사실 확인이 필요하면 MCP 도구를 먼저 써봐라 — 허용 목록과 무관하게 돈다.

### 3-4. 아직 원인을 모르는 것 두 개

| 건물 | 요청 동 | 행 | 상태 |
|---|---|---|---|
| 삼척 원조아파트 (사직동 319-1) | 201동 | 140행 | 네이버에 201동이 없다. 등기부 실제 표기를 봐야 안다. |
| 아산 한도아파트 (배방읍 공수리 282-5) | 106동 | 131행 | 같음 |

**추측으로 규칙을 넣지 마라.** 브리지에서 실제 표기를 받아본 뒤에 판단할 것.

---

## 4. E2E 테스트의 한계 — 반드시 알고 있어야 한다

`npm run test:iros`는 브라우저를 띄워 **진짜 앱**을 돌리지만,
`/api/juso` · `/api/naver` · `/api/resolve`는 **목업**이다.
그리고 `scripts/iros-e2e-cases.mjs`의 등기부 후보는 **내가 만든 합성값**이다.

즉 이 테스트가 증명하는 것은 **"매칭 로직이 설계대로 동작한다"**이지
**"등기부가 실제로 그렇게 적는다"**가 아니다. 회귀는 잡지만 가정이 맞는지는
못 잡는다. 그래서 `probe:iros`가 필요하다.

---

## 5. 다음에 할 일 (순서대로)

### 0단계 — 네트워크가 열렸는지 먼저 확인

```bash
for h in www.iros.go.kr business.juso.go.kr openapi.naver.com unisquads.vercel.app; do
  echo -n "$h -> "; curl -sS -o /dev/null -m 20 -w "%{http_code}\n" "https://$h/" 2>&1 | tail -1
done
curl -sS "$HTTPS_PROXY/__agentproxy/status"   # 막혔으면 사유가 여기 찍힌다
```

403이면 아직 안 열린 것이다. 환경(cloud environment)의 Network access를
**Custom**으로 두고 위 호스트를 Allowed domains에 넣은 뒤, **그 환경으로 새
세션을 시작**해야 적용된다. 실행 중인 세션에는 반영되지 않는다.
`Also include default list of common package managers`를 반드시 체크할 것 —
안 하면 npm·github까지 막힌다.

### 1단계 — 실제 등기 브리지에서 표기를 받아온다

```bash
npm run probe:iros -- --file scripts/iros-cases.example.txt --bridge <브리지주소> --save real.json
```

`scripts/iros-cases.example.txt`에 실패 상위 14곳(미해결 1,860행 중 1,304행,
70%)이 들어 있다. 주소·동·호는 그 실행이 실제로 브리지에 보낸 값 그대로다.

프로브는 등기부의 실제 소재지·건물명·구분·동·호 분포를 찍고, 이어서 매칭
단계를 재생하며 후보가 어디서 잘리는지 보여준다.

**볼 것 세 가지**
1. 등기부가 쓰는 동·호 표기가 우리 가정과 맞나 (`301` ↔ `제3층 제1호`)
2. 등기 소재지가 확정 지번과 같나 (삼본아파트는 다르다)
3. 후보 0건이면 조회 문구 자체가 틀린 것이다

### 2단계 — 합성값을 실측값으로 교체

`real.json`으로 `scripts/iros-e2e-cases.mjs`의 후보를 교체한다. 그러면 E2E가
"설계대로 동작한다"에서 "실제 등기 표기에서 동작한다"로 올라간다.

### 3단계 — 원인 미상 두 건 판정

3-4의 삼척·아산. 프로브 결과를 보고 규칙을 넣을지 실패로 확정할지 정한다.

### 4단계 — 다시 전량 실행해서 진짜 회수율을 잰다

지금까지의 IROS 수치는 전부 "대상"이다. 재실행 전까지 복구율을 단정하지 마라.

---

## 6. 새 세션에서 사라지는 것

- **업로드한 xlsx 스냅샷.** 20,023행 실행 결과는 세션 업로드 파일이라 남지
  않는다. 측정을 다시 하려면 **`____PARTIAL.xlsx`를 새 세션에 다시 올려야 한다.**
  (실제 고객 주소라 레포에 커밋하지 않았다.)
- 스크래치패드의 시뮬레이션 스크립트 전부.

레포에 남아 있는 것: 소스, 테스트, 프로브, 케이스 파일, 이 문서.

---

## 7. 확인 못 한 것 — 넘겨받는 사람이 봐야 한다

- **Vercel 배포본이 v1.7.2인지 확인 못 했다.** 사이트에 도달할 수 없었다.
  앱 헤더의 버전 표기로 확인할 것. 화면에는 릴리스 시각과 배포 시각이 따로
  찍힌다("고쳤다"와 "배포됐다"는 다른 사실이라 일부러 나눠 놨다).
  → **해소(08-07)**: 배포본 v1.7.3 = 당시 main과 일치 확인.
- **`newwonwoo-unisquads` Vercel 프로젝트는 모든 PR에서 실패한다.** #40 이전부터
  그랬다. 이 프로젝트를 실제로 쓰는지 확인이 필요하다.
- **GitHub Actions 러너가 한동안 멈춰 있었다.** 큐에 1시간 이상 머물다 전부
  cancelled 됐고, 재실행 API는 `403 Resource not accessible by integration`을
  냈다. #41·#42는 로컬 검증만으로 머지했다(사용자 승인).

---

## 8. 실측 프로브 결과 (2026-08-07) — 실패 상위 14곳 전수

네트워크 개통 후 `npm run probe:iros -- --file scripts/iros-cases.example.txt
--bridge https://unisquads.vercel.app/api` 로 14곳 전부 실제 등기부 후보를
받았다. 판정:

**이미 해결 (v1.7.x 규칙이 실제로 동작) — 373행**
| 건물 | 행 | 경로 |
|---|---|---|
| 동해 삼본 | 194 | 정확 매칭 (소재지 역확정) |
| 광주 동산 | 134 | 정확 매칭 (#42 지번 유실 수정) |
| 경주 청우 | 45 | R-IROS-DONG-AGNOSTIC-HO (등기부는 전부 301동, 건물명 청우타운) |

**새 규칙으로 회수 (§9) — 276행 대상**
| 건물 | 행 | 실측 사실 |
|---|---|---|
| 부산 범일역풍림아이원 | 46 | 요청 세대 실존, 호가 `"103동902"`(자기 동 접두) |
| 청주 진흥 | 136 | 동101·호"1"이 6건, **floor 필드 1~6**으로만 구분 (원문 "3층1호") |
| 횡성 서도1차 | 94 | 세대는 전부 무동, 같은 호의 **상가동** 세대가 유일성을 깸 |

**요청 동이 그 지번 등기부에 없음 (3-4의 미상 2건 포함 원인 확정)**
| 건물 | 행 | 실측 사실 |
|---|---|---|
| 삼척 원조 (201동) | 140 | 사직동 319 전체에 101·102동뿐 (건물명 "원조임대아파트") |
| 천안 중앙 (101동) | 142 | 상동리 91 전체에 102·103동뿐 |
| 아산한도 (106동) | 131 | 101~105·107동만. **주의: 브리지가 옆 단지 배방삼정그린코아 106동을 오확정** — 로컬 건물명 게이트가 막았다. 이 게이트 완화 금지 |
| 영천 창신 (109동) | 44 | 등기부는 101~108동. 네이버에 "창신영천타운 109동방면입구" 실존. 이웃 지번 471-7에 이름 없는 18세대 건물 있음(109동일 개연성, **확증 없음 — 추측 확정 금지**) |

**주소/조회 문제 — 매칭으로 못 푼다**
| 건물 | 행 | 실측 사실 |
|---|---|---|
| 전주 효자동2가 | 74 | 확정 지번(1236-4) 등기부엔 무동 401~403호뿐. 원문 지번은 250 — 주소 확정 자체 의심 |
| 전주 덕일빌리지 | 48 | 후보 0건. 조회 지번(126-4)이 등기 소재지와 다름 |
| 포항 원리 893-14 | 36 | 그 지번엔 집합건물 없음. 모지번 893에 "외 2필지" 무명 41세대 건물 존재(근거 보강 필요) |
| 전주 한신휴플러스 | 40 | 등기부에 106동 102호 실존 — 기존 중복 동 규칙이 raw를 받으면 해결(E2E 실측 검증 완료) |

브리지 응답의 `all_candidates`가 전체 후보이고 `candidates`는 확정건만이다.
프로브 원본은 세션 스크래치패드 `real.json`이라 세션 종료 시 사라진다 —
필요하면 다시 돌리면 된다(14곳 약 30초).

## 9. v1.8.0에서 넣은 규칙 4종 — 전부 실측 근거

| 규칙 | 대상 | 내용 |
|---|---|---|
| self_dong_ho_prefix (`candidateUnitVariants`) | 46행 | 호 `"103동902"`에서 접두어가 **그 후보 자신의 동과 일치할 때만** 분해 |
| `selectFloorDisambiguatedCandidate` (R-IROS-FLOOR-DISAMBIG) | 136행 | 동·호 동일 복수후보를 원문 층 ↔ 등기부 floor 필드로 유일화. 지하 제외, 층 없으면 불개입 |
| `excludeShopDongForDonglessRequest` (R-IROS-SHOP-DONG-EXCLUSION) | (94행 중 동 미전파분) | 동 없는 요청에서 같은 호가 무동+상가동에만 갈리면 상가 배제. 숫자·알파벳 동이 섞이면 배제 안 함 |
| `selectDongAgnosticHoCandidate` v2 | 94행 | 최후수단 동 무시 매칭에서 명시적 이질 동(상가)은 유일성을 깨지 않음. 무동 후보가 있으면 그 안에서만 유일성 판정 |

매처 버전 v11→v12, `failure-recovery-plan.mjs`에 재판정 승격 선택자 3종
추가(`needsSelfDongHoPrefixRematch`, `needsFloorDisambigRematch`,
`needsShopDongExclusionRematch`). 순서 계약(DONG-AGNOSTIC-HO 최후수단) 유지 —
`tests/test_pnuless_iros.mjs`가 계속 강제한다.

E2E 후보는 합성값에서 **실측 고유번호·표기**로 교체했다(진흥·서도·한신·청우
+ 신규 범일). 결과: 단위·계약 320개 통과, E2E 13행/0실패.

## 9-2. v1.9.0 — 주소 실패 복구 모듈 4종 (2026-08-07, 전부 실측 근거)

20,023행 주소정제 결과(xlsx)의 미회수 군집을 라이브 프로브로 분해해 넣었다.
새 모듈 `public/address-failed-requery.mjs` + `pnuless-iros.mjs` v2.
전부 실패 상태에서만 발화한다 — CONFIRMED 행은 재조회 경로에 진입 자체가 안 된다.

| 모듈 | 실측 근거 | 대상(실측 군집) |
|---|---|---|
| R-ADDR-BUILDING-NAME-LOT | JUSO `detBdNmList`가 동↔지번 매핑 제공(1동=1082, 6동=1086처럼 동별 필지 분리 상가). 원문 동이 실존하는 지번 하나로만 확정, 못 좁히면 AMBIGUOUS로 복수PNU-IROS에 인계 | 만수동 상가 153행 |
| R-ADDR-EXCLUDED-LOT | 용도 불일치로 배제한 후보(유치원)의 지번에 원문 건물명과 일치하는 주거 건물 실존(JUSO+IROS 30동1102 확정 실측) | 동해 대동 52행 |
| R-ADDR-NAVER-LOT-RESCUE | 네이버가 지번 없는 신행정구역 주소만 준 행. 읍면동+건물명 지역검색 → 주거 카테고리 지번 → JUSO 교차확인(용당동 748 실측) | 순천 122행 |
| R-IROS-PNULESS rawLot(v2) | JUSO 0건 지번이 등기부엔 완전수집 존재(내북면 148-3: 82세대, 정확 매칭 확정 실측). 원문에 시도+시군구+지번+동+호 완비 행만 | 보은 81행 |

실패확정으로 판정한 군집(규칙 금지): 문경 모전동 범위지번 116행(건물명
없음 + 범위 안에 복수 단지), 임실 신우아파트 192행(등기부에 아파트 등기
자체가 없음). 울산 삼산동 199행은 이웃 지번(1498-2 등) 추가 프로브 필요.

참고: 이 xlsx의 "자동 재처리 대상 1,392"는 계획(route)만 기록된 큐였고
실행기가 없었다. v1.9.0은 재처리를 별도 실행이 아니라 **1차 정제 실패
직후 인라인으로** 시도한다 — 재실행하면 바로 반영된다.

## 9-3. v1.10.0 — 등기 전량 실행(v11 기준) 분석과 무기재 건물명 게이트 (2026-08-07)

20,023행 전량 등기 실행 결과(iros-matcher-v11 = 배포 v1.7.3 기준):
**추출율 84.56%** (16,932행, 고유 기준 84.99%). 미회수 3,091행.

**민감도 실측**: 신규 규칙 중 기존 확정을 건드릴 수 있는 유일 지점(동 접두
호 분해의 변형 추가)을 저장된 실측 후보 7,153건 전체로 검사 — 분해 대상
223건 중 기존 평문 (동,호)와 충돌 **0건**. 나머지 규칙은 전부 실패 상태에서만
발화하고, RESOLVED+고유번호는 `isProtectedIrosSuccess`가 보호한다.

**최대 발견 — R-IROS-NAMELESS-REGISTRY-EXACT (457행)**: 어달동(삼본) 194 +
농성동(동산) 263이 완전수집·동·호 정확 매칭까지 성공하고도 검토 게이트의
건물명 교차검증에서 죽어 있었다. 등기부가 건물명을 아예 안 적는 단지라
교차검증이 원천 불가능. 후보 건물명이 빈값이고 확정 지번 위 정확 매칭이
유일한 한 건일 때만 통과시킨다. 건물명이 적혀 있으면 절대 열리지 않아
아산한도 옆단지 오확정 방어는 그대로다. 매처 v12→v13.

v11 실행 기준 신규 규칙(v1.8.0~v1.10.0)의 실측 검증 회수 대상:
| 대상 | 행 | 경로 |
|---|---|---|
| 무기재 건물명 게이트 | 457 | v1.10.0 (정답 고유번호 프로브 확인) |
| 진흥 복수결과 | 136 | v1.8.0 층 유일화 |
| 범일 세대미일치 | 46 | v1.8.0 동 접두 호 |
| 주소 4모듈 | 408 | v1.9.0 (보은81·창대153·대동52·용당122) |
| 합계 | **1,047** | 84.56% → 산술 상한 **약 89.8%** |

미회수 잔여 중 원인 확정·미해결:
- 요청 동이 등기부에 없음: 원조 116 + 중앙 80 + 아산한도 98 + 창신 42 = 336행
  (이웃 지번·외부 근거 없이는 실패확정이 맞다)
- 갈산동 하나상가 25행: "지1-1"(지하1층 1호)을 동1·호1로 오파싱 + 건물을
  동남아파트로 오확정 — 지하 표기 파싱 확장 + 건물명 우선 확정 검토 필요
- 길동 한신휴플러스 21행: 231건 완전수집에서 1동 202호 미일치 — 표기 프로브 필요
- 오천읍 동소 36행: 동소 치환 후 건물명 교차검증 실패 — 후보 표기 프로브 필요
- 나머지 세대미일치 롱테일 ~400행: 5행 이하 소군집 다수

## 9-4. "요청 동 부재" 재판정 (2026-08-07 저녁, 실측)

"등기부에 그 동이 없다 → 실패확정"이라 판정했던 A군집(337행)을 재검증한
결과, **판정이 틀린 곳이 확인됐다**. JUSO 건물명 검색의 detBdNmList가
동↔지번 매핑을 준다(창대장터에서 확립한 방법):

- **창신타운 109·110동 = 망정동 462-2** (확정 지번 471이 아님!). 등기부
  462-2에 완전수집 존재, 109동 101호 = 1747-1996-197462 **정확 매칭 확증**.
  코퍼스에 반영됨(25번째 지번). → 42행 회수 경로 확보
- 아산한도 106동·중앙 101동: JUSO 동목록에는 실존하는데 그 지번 등기부엔
  없다 — 등기부 동표기 오기재/합본 가능성. 후보 호수 분포 정밀 비교 필요
- 원조 201동: JUSO에도 없음 — 원문 "201-101"의 201이 동이 아닐 가능성
  (전처리 해석 재검토 대상)

**후속 정밀 분석(2026-08-08 심야, 전부 실측)**:
- **용당 12동(120행) = 실패확정.** 요청 호가 120종인데 등기부 어느 동도
  120호를 못 담는다(최대 113동 111세대·108동 114세대, 커버리지 최고 79%).
  JUSO 동목록(748)에도 12동·104동은 없다. 12→112든 12→102든 대량 오확정 —
  v1.16.2 커버리지 가드(80%)가 정확히 전부 기권시킨다.
- **중앙 101동(71행) = 실패확정 + 기존 확정도 오염.** JUSO는 101~105동
  실존을 확인하는데 등기부는 102(266세대)·103(98세대)만 적는다(합본 기재).
  동무시 호유일이 요청 101·104·105동 61행을 전부 등기 102동으로 확정했고,
  그 결과 **서로 다른 요청 동이 같은 고유번호를 나눠 가진 사례가 32건**
  이미 존재한다. 실패 101동 71행의 호 59종은 성공한 102동 요청과 겹쳐,
  앵커 매핑(101→102)이 발화하면 59행이 추가 오확정될 뻔했다 —
  v1.17.1 충돌 가드(대상 동이 직접 요청되면 기권 + 두 요청 동이 한 등기부
  동으로 수렴하면 폐기)가 이를 배포 전에 차단한다.
- 아산한도 106동: 이전 분석 유지 — 등기부에 106동이 실존하나(호 501~520,
  "외 3종"에 가려져 있었음) 101동과 (동,호) 중복 25쌍이라 매핑 불가,
  실패확정.

**구현됨(v1.17.0)**: `public/dong-lot-relocate.mjs` — REG_UNIT_NOT_FOUND
완전수집 + 요청 동이 후보에 없음 + `result.bdNm`으로 JUSO 건물명 재검색 →
detBdNmList에 요청 동이 실린 지번이 정확히 하나이고 현재 지번과 다르면
기존 대체지번 큐(`addAlternateMember`)로 IROS 재조회. 확정은 기존 수렴
로직이 하고 태그만 `R-IROS-DONG-LOT-RELOCATE@1`로 구분. 기권 반례 9종
단위 고정, E2E "창신타운 · 동↔지번 재배치" 케이스(실측 JUSO 응답 + 코퍼스
후보)로 앱 전체 흐름 검증. 명시 대체지번이 이미 걸린 행은 기권(원문 우선),
재시도 멱등(`reg.dong_lot_relocate` 표식). 부수: 용당 "12동"→112동 접미
매핑도 같은 detBdNmList 근거로 검증 가능(미구현). 감사 게이트 5종 통과.

## 10. 다음 단계 (갱신)

1. **전량 재실행으로 진짜 회수율 측정** — `____PARTIAL.xlsx`를 다시 올려서.
   §8 기준 산술 상한: 이미 해결 373 + 새 규칙 대상 276 = 649행 / 미해결
   1,860행. 상위 14곳 밖 556행에도 같은 유형이 있으면 더 나온다.
2. **동 없는 지번 4곳(원조·중앙·아산한도·창신)** — 이웃 지번 재조회로 풀려면
   등기부 `add_item`("외 N필지")·건물명 일치 같은 실증 근거가 필요하다.
   추측 규칙 금지 원칙 유지. 근거가 안 나오면 실패 확정이 맞다.
3. **주소 쪽 3곳(효자동2가·덕일·포항)** — 주소 확정 단계 재검토 대상.
4. 참고: 이 환경은 npm이 막혀 있으면 `node_modules`에 시스템 playwright
   (`/opt/node22/lib/node_modules`) 심링크 + CDN에서 react/xlsx UMD를 받아
   E2E를 돌릴 수 있다. Node fetch는 `NODE_USE_ENV_PROXY=1`이 필요하다.
